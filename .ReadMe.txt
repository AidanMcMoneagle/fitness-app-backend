New Workout Data will be an array of objects. 

// How data for each workout will be sent.
[
    {
        id: .......,
        value: {
            excerciseName: '',
            repetitions: '',
            sets: ''
        }
    }, 
    {

    }, 
    {

    }
]

How Data should be stored in DB...

import mongoose from "mongoose" 
const Schema = mongoose.Schema 
const WorkoutSchema = new mongoose.Schema({ userId: { type: Schema.ObjectId, ref: "User" }, 
exercises: [ { name: String, sets: Number, reps: Number, } ], }, ) 

export const Workout = mongoose.model("Workout", WorkoutSchema)


